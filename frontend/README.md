# Frontend

To install dependencies:

```bash
# Make sure you install the backend dependencies first
# See ../backend/README.md
bun install
cp template.env .env
```

To run:

```bash
# Make sure the backend is running first
# See ../backend/README.md
bun run dev
```

To build:

```bash
bun run build
```
